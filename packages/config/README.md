# Shared Configuration

## Feature Flags

`feature-flags.json` controls optional platform features. The API reads this file at runtime (path configurable via `FEATURE_FLAGS_PATH`).

Toggle flags to test agent behavior when features are disabled.